@extends('layouts.admin')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h2 class="section-heading mb-1">Vulnerable Areas & Households</h2><p class="section-excerpt mb-0">Maintain disaster-risk records for vulnerable communities and household clusters.</p></div><a href="{{ route('admin.vulnerable-areas.create') }}" class="btn btn-primary">Add Record</a></div>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="card border-0 shadow-sm mb-4"><div class="card-body"><form class="row g-2"><div class="col-md-6"><input name="search" value="{{ request('search') }}" class="form-control" placeholder="Search area, household cluster, address"></div><div class="col-md-3"><select name="level" class="form-select"><option value="">All Vulnerability Levels</option>@foreach(['Low','Medium','High','Critical'] as $level)<option value="{{ $level }}" @selected(request('level')===$level)>{{ $level }}</option>@endforeach</select></div><div class="col-md-3"><button class="btn btn-outline-primary w-100">Search</button></div></form></div></div>
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead class="table-light"><tr><th>Name</th><th>Type</th><th>Address</th><th>Households</th><th>Population</th><th>Risk</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($areas as $area)<tr><td class="fw-semibold">{{ $area->name }}</td><td>{{ $area->area_type }}</td><td>{{ $area->address }}</td><td>{{ $area->household_count }}</td><td>{{ $area->population_count }}</td><td><span class="badge bg-{{ $area->vulnerability_level==='Critical'?'danger':($area->vulnerability_level==='High'?'warning text-dark':($area->vulnerability_level==='Medium'?'info text-dark':'success')) }}">{{ $area->vulnerability_level }}</span></td><td>{{ ucfirst($area->status) }}</td><td><a href="{{ route('admin.vulnerable-areas.edit',$area) }}" class="btn btn-sm btn-outline-primary">Edit</a>@if($area->status==='active')<form class="d-inline" method="POST" action="{{ route('admin.vulnerable-areas.deactivate',$area) }}">@csrf<button class="btn btn-sm btn-outline-secondary">Deactivate</button></form>@endif</td></tr>@empty<tr><td colspan="8" class="text-center text-muted py-4">No vulnerable area records yet.</td></tr>@endforelse</tbody></table></div></div>
@endsection
