@extends('layouts.admin')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-4"><div><h2 class="section-heading mb-1">Disaster Response Equipment Inventory</h2><p class="section-excerpt mb-0">Record, monitor, and update emergency response equipment and supplies.</p></div><a href="{{ route('admin.response-equipment.create') }}" class="btn btn-primary">Add Equipment</a></div>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="card border-0 shadow-sm mb-4"><div class="card-body"><form class="row g-2"><div class="col-md-9"><input name="search" value="{{ request('search') }}" class="form-control" placeholder="Search equipment, category, serial number, storage location"></div><div class="col-md-3"><button class="btn btn-outline-primary w-100">Search</button></div></form></div></div>
<div class="card border-0 shadow-sm"><div class="table-responsive"><table class="table align-middle mb-0"><thead class="table-light"><tr><th>Equipment</th><th>Category</th><th>Serial</th><th>Qty</th><th>Condition</th><th>Status</th><th>Storage</th><th>Action</th></tr></thead><tbody>@forelse($equipment as $item)<tr><td class="fw-semibold">{{ $item->name }}</td><td>{{ $item->category }}</td><td>{{ $item->serial_number ?: 'N/A' }}</td><td>{{ $item->quantity }}</td><td>{{ $item->condition }}</td><td><span class="badge bg-{{ $item->status==='available'?'success':($item->status==='maintenance'?'warning text-dark':'secondary') }}">{{ ucfirst(str_replace('_',' ',$item->status)) }}</span></td><td>{{ $item->storage_location ?: 'N/A' }}</td><td><a href="{{ route('admin.response-equipment.edit',$item) }}" class="btn btn-sm btn-outline-primary">Edit</a>@if($item->status!=='inactive')<form class="d-inline" method="POST" action="{{ route('admin.response-equipment.deactivate',$item) }}">@csrf<button class="btn btn-sm btn-outline-secondary">Mark Inactive</button></form>@endif</td></tr>@empty<tr><td colspan="8" class="text-center text-muted py-4">No equipment records yet.</td></tr>@endforelse</tbody></table></div></div>
@endsection
